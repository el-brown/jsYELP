// PRINT ALL EVEN NUMBERS 0-10 (OR MORE)
const printEvens = (num) => {
  for ( i = 1 ; i <= num; i++ )
    (i % 2 === 0) && console.log(i) 
}

printEvens(20) //2 4 6 8 10

// PRINT A TABLE CONTAINING MULTIPLICATION TABLES

const multTable = (num) => {
  
}

multTable(10)